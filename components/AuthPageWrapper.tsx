interface AuthPageWrapperProps {
   icon: JSX.Element;
}

const AuthPageWrapper: React.FC<AuthPageWrapperProps> = ({ icon }) => {
   return <div className="flex flex-col w-full h-full mt-12 my"></div>;
};

export default AuthPageWrapper;
